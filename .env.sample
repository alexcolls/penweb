# Environment variables for penweb project

# Logging Configuration
# Set to 'true' to enable file logging in logs/ directory
# Logs will be saved with timestamp format: YYYY-MM-DD_HH-MM-SS.log
SAVE_LOGS=false

# Output Directory Configuration
# Directory where all CLI outputs will be saved (cloned sites, reports, etc.)
# This directory is automatically added to .gitignore
OUTPUT_DIR=.output

# AWS CREDENTIALS
AWS_ID=
AWS_KEY=

