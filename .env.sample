# Environment variables for penweb project

# Logging Configuration
# Set to 'true' to enable file logging in logs/ directory
# Logs will be saved with timestamp format: YYYY-MM-DD_HH-MM-SS.log
SAVE_LOGS=false

# Output Directory Configuration
# Directory where all CLI outputs will be saved (cloned sites, reports, etc.)
# This directory is automatically added to .gitignore
OUTPUT_DIR=.output

# AWS CREDENTIALS
AWS_ID=
AWS_KEY=

# ============================================
# 📦 INTEGRATED CLI TOOLS CONFIGURATION
# ============================================
# The following tools are integrated as git submodules and have their own
# configuration files. All tools work with FREE defaults requiring NO account!
#
# 🛠️ GPS-CLI (Device Location Tracker)
#   Config: ~/.config/gps-cli/.env.sample
#   Default: FREE Traccar demo server (https://demo2.traccar.org)
#   Features: IP-based geolocation, no GPS hardware needed
#   Setup: Run `./run.sh` and select option 1
#
# 🔐 VPN-CLI (Multi-Provider VPN Manager)
#   Config: ~/.config/vpn-cli/config.json (auto-created on first run)
#   Default: FREE VPNGate public servers (no account needed)
#   Features: Multiple countries, instant connection
#   Setup: Run `./run.sh` and select option 2
#
# 📧 EMAIL-CLI (Temporary Email Manager)
#   Config: ~/.config/tempmail/.env.sample
#   Default: FREE 1secmail.com API (no account needed)
#   Features: Unlimited temp emails, instant inbox access
#   Note: SMTP config only needed for SENDING emails (optional)
#   Setup: Run `./run.sh` and select option 3
#
# 💡 TIP: All tools work immediately after installation with NO configuration!
#      The .env.sample files are templates for advanced customization only.
# ============================================

